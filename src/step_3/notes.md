## Split the loop

It may affect the performance, so use it carefully.
Optimization requires accurate performance measurements.

Create the method `getTotalFrequentRenterPoints`.

Create the method `getTotalCharge`.

